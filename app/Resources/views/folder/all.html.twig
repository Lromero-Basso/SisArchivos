{% extends "base.html.twig" %}

{% block title %} {{ parent() }} - Cajas {% endblock %}

{% block body %}

<div class="row"> 
  <!-- FILTERING -->
  <div class="col-md-12"> 
    <div  id="filters" class="collapse"> 
      <form class="card pl-3 pr-3 pt-2 bg-light mb-3" action="{{ path('viewFolders') }}" method="get">
        <div class="row">
          <div class="col-md-1">{{ form_row(filterForm.id) }}</div>

          <div class="col-md-1">{{ form_row(filterForm.nroCarpeta) }}</div>
          <div class="col-md-2">{{ form_row(filterForm.codCaja) }}</div>
          <div class="col-md-2">{{ form_row(filterForm.tituloCarp) }}</div>
          <div class="col-md-2">{{ form_row(filterForm.estado) }}</div>

          {# INICIO Render manual date range#}

          <div id="fecha" class="col-md-5">
              <div class="row">
                  <div class="col-md-6">
                      <div class="form-group">
                          <label class="control-label" for="fecha_left_date">Fecha desde</label>
                          <input type="date" id="fecha_left_date" name="fecha[left_date]" class="form-control" value="{{ filterForm.fechaDesdeCarp.children.left_date.vars.value}}" />
                      </div>
                  </div>
              
                  <div class="col-md-6">
                      <div class="form-group">
                          <label class="control-label" for="fecha_right_date">fecha hasta</label>
                          <input type="date" id="fecha_right_date" name="fecha[right_date]" class="form-control" value="{{ filterForm.fechaHastaCarp.children.right_date.vars.value}}"/>
                      </div>
                  </div>
              </div>
          </div> 
          {% do filterForm.fechaDesdeCarp.setRendered %}
          {% do filterForm.fechaHastaCarp.setRendered %}
          {# FIN Render manual date range#}

    

          <div class="col-md-6">
            {{ form_rest(filterForm) }}
          </div>

          <div class="col-md-12">
              <div class="form-group">
                  <button type="submit" class="btn btn-warning" name="filter_action" value="filter"><i class="fas fa-filter"></i> Filtrar</button>
                  <button type="submit" class="btn btn-secondary" name="filter_action" value="reset"><i class="fas fa-minus"></i> Reset</button>
              </div>
          </div>
        </div>

      </form>
    </div>
  </div>
  <!-- END FILTERING -->


  <!-- PAGE SIZE -->
  <div class="col-md-3 pull-left">
    <div class="pagination form-inline ">
      <select class = "form-control"  onchange="window.location = this.value" >
          <option value='{{ path('viewFolders', app.request.query.all|merge({'pcg_show': '10'})) }}' {% if app.request.get('pcg_show')  == 10 %}  selected {% endif %}>10</option>
          <option value='{{ path('viewFolders', app.request.query.all|merge({'pcg_show': '20'})) }}' {% if app.request.get('pcg_show')  == 20 %}  selected {% endif %}>20</option>
          <option value='{{ path('viewFolders', app.request.query.all|merge({'pcg_show': '50'})) }}' {% if app.request.get('pcg_show')  == 50 %}  selected {% endif %}>50</option>
          <option value='{{ path('viewFolders', app.request.query.all|merge({'pcg_show': '100'})) }}' {% if app.request.get('pcg_show') == 100 %} selected {% endif %}>100</option>
          <option value='{{ path('viewFolders', app.request.query.all|merge({'pcg_show': '500'})) }}' {% if app.request.get('pcg_show') == 500 %} selected {% endif %}>500</option>
      </select>
    </div>
      
  </div>

  <!-- END PAGE SIZE -->

  <div class="col-md-6"></div>

  <!-- FILTER BUTTON -->
  <div class="col-md-3">
    <div class="float-right">
      <a class="btn dropdown-toggle pull-right h3" data-toggle="collapse" data-target="#filters">Filtro<span class="caret"></span></a>	
      <a class="btn btn-primary h3 pull-right" href="{{ path('createFolder') }}" style="margin-bottom:10px"><i class="fas fa-plus"></i> Nuevo</a>
    </div>	
  </div>
  <!-- END FILTER BUTTON -->

  <!-- FORM ERRORS -->
  <div class="col-md-12">
        {%- if form_errors(filterForm) %}
        <div class="alert alert-block alert-error fade in form-errors">
            {{ form_errors(filterForm) }}
        </div>
        {% endif %}
  </div>
  <!-- FORM ERRORS -->

</div>

<!-- FLASH MESSAGES -->
{# <div class="row">
    <div class="col-md-12">
        {% for type, flashMessages in app.session.flashbag.all() %} 
        {% for flashMessage in flashMessages %}
        <div class="alert alert-{{ type }}">
            {{ flashMessage|raw }}
        </div>
         {% endfor %} 
         {% endfor %} 
    </div>
</div> 

Poner los flash en el base

#}
<!-- END FLASH MESSAGES -->

{# <form method="POST" id="bulk_form" action="{{ path('carpecaja_bulk_action') }} ">
  <div class="card mb-4 mt-4">
    <table class="table table-striped table-hover table-sm" id="tabla" namefile="Carpecaja">
      <thead>
        <tr>
          {% import "@PetkoparaCrudGenerator/macros/th_sortable.html.twig" as macros %}
          <th style="border:0px;">{{macros.th_sortable('id',app.request.get('pcg_sort_col'), app.request.get('pcg_sort_order') , 'carpecaja','Id')}}</th>
          <th style="border:0px;"></th>
          <th style="border:0px;"></th>
          <th style="border:0px;"></th>
          <th style="border:0px;"></th>
        </tr>
      </thead>
    </table>
  </div>
</form> #}

{% endblock %}
